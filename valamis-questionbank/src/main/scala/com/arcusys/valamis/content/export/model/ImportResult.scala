package com.arcusys.valamis.content.export.model

case class ImportResult(allCount: Int,
                        okCount: Int,
                        errorCount: Int,
                        skippedCount: Int)
